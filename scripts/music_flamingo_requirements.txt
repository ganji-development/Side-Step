# Dependencies for scripts/music_flamingo_server.py
#
# Install into a SEPARATE venv from Side-Step. Side-Step pins
# transformers<4.58 (pyproject.toml), while MusicFlamingoForConditionalGeneration
# needs a much newer release — sharing a venv breaks the trainer.
#
# torch is installed separately from the CUDA index; see
# run_music_flamingo_server.sh.

transformers>=4.60.0
accelerate>=1.12.0

# HTTP layer
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.9

# Audio decoding for the processor's audio loading path
soundfile>=0.13.1
librosa>=0.10.0
